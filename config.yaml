endpoint:
  base_url: "http://localhost:8000/v1"
  api_key: "dummy"

model: "mistralai/Mistral-7B-Instruct"

sampling:
  temperature: 0.0
  max_tokens: 512
  stream: false

run:
  seed: 7
  dataset: "data/smoke.csv"
  output_dir: "reports"
  run_name: "mistral7b_fp16_bs64"
  parallelism: 8

scoring:
  default: "regex"
  json_schema: null

perf:
  enabled: true
  lengths: [20, 200, 1000]
