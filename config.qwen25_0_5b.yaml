model: "qwen2.5:0.5b-instruct"

sampling:
  temperature: 0.0
  max_tokens: 64
  stream: false

run:
  seed: 7
  dataset: "data/xstest_full.csv"
  output_dir: "reports"
  run_name: "ans_qwen25_0_5b"
  parallelism: 4  # adjust if you see OOM; 2–4 is safe for small models

scoring:
  default: "regex"
  json_schema: null

perf:
  enabled: true
  lengths: [20, 200, 1000]